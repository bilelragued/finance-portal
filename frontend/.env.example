# Frontend Environment Variables

# API URL - Leave empty for production (will use relative /api)
# For local development with separate backend:
# VITE_API_URL=http://localhost:8000/api

VITE_API_URL=
